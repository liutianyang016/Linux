#include <stdio.h>
#include <stdlib.h>
#include "menu.h"
#include "database.h"

int main() {
    // 初始化数据库
    if(init_database() != 0) {
        fprintf(stderr, "Failed to initialize database\n");
        return 1;
    }
    
    // 显示主菜单
    show_main_menu();
    
    // 关闭数据库
    close_database();
    
    return 0;
}